#include <iostream>
#include <map>
#include <set>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false); cin.tie(0);
    int n;
    int count = 0;
    cin>>n;
    map <int,int> xtoy;
    map <int,int> ytox;
    for (int i = 0 ;i<n;i++){
        int a;
        cin>>a;
        if (a == 1){
            int x,y;
            cin >> x>>y;q
            auto left  = xtoy.lower_bound(x);
            auto right = ytox.lower_bound(y);
            if (left == end() || left->second =)
        }eles{
            
        }
    }
    return 0;
}